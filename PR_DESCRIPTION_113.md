# PR Description — 增加 agent / command 关联文件同步能力

## Summary
本 PR 为 skills-manager 增加“关联资源同步”能力：当某些 skill 依赖 `agent` 或 `command` 配置文件时，支持在同步 skill 的同时一并同步这些关联目录中的文件，避免“skill 已同步但运行依赖缺失”的问题。

Closes #113.

## 背景与问题
当前同步逻辑主要聚焦 skill 目录本身。对于需要配套 `agent` 或 `command` 才能正常使用的 skill，若关联文件位于其他目录，则不会被一起同步，导致：
- 目标环境中 skill 可见但不可用；
- 用户需要手工补齐依赖文件；
- 多环境迁移和团队协作体验变差。

## 目标
1. 支持 skill 与 `agent` / `command` 的关联同步。
2. 保持默认行为兼容，不破坏现有仅 skill 同步流程。
3. 同步结果可观测（日志/状态可定位哪些关联文件已同步、哪些被跳过）。

## 非目标
- 不改变现有 skill 文件格式的核心语义。
- 不在本次引入复杂的远端执行或插件运行时行为。

## 方案概述
1. **关联声明识别**
   - 解析 skill 元信息中的关联项（例如依赖到 agent/command 的路径或标识）。
2. **同步规划扩展**
   - 在生成同步计划时，追加关联资源任务（agent/command 文件）。
3. **执行与冲突策略**
   - 统一走现有同步管线，复用覆盖/跳过策略；
   - 对不存在路径、权限不足、循环依赖等场景做显式报错或告警。
4. **可观测性**
   - 输出同步摘要：skill 数量、关联文件数量、成功/失败明细。

## 兼容性
- 默认保持向后兼容：未声明关联资源的 skill 行为不变。
- 对旧项目零侵入，仅在检测到关联资源时触发扩展同步。

## 风险与缓解
- **风险**：关联路径配置错误导致同步失败或误同步。
  - **缓解**：增加路径合法性校验与清晰错误提示。
- **风险**：同步范围扩大带来性能开销。
  - **缓解**：仅在存在关联声明时执行；可复用增量/去重逻辑。
- **风险**：目录穿越或越权路径。
  - **缓解**：限制在允许的工作区根目录内解析与访问。

## 验证计划
- 单元测试：
  - 关联声明解析正确性；
  - 同步计划中包含 agent/command 关联项；
  - 异常场景（缺失文件、非法路径）处理。
- 集成测试：
  - 含关联资源的 skill 同步后可直接使用；
  - 无关联资源的旧用例行为保持不变。
- 手工验证：
  1. 准备一个依赖 agent/command 的 skill；
  2. 执行同步；
  3. 校验目标目录下 skill 与关联资源均已就位。

## 发布与文档
- 更新使用文档，说明如何声明并同步关联资源。
- 在发布说明中标注“新增 agent/command 关联同步能力”。

## Checklist
- [x] Full local PR description drafted.
- [ ] 功能实现完成。
- [ ] 测试补充并通过。
- [ ] 文档更新完成。
